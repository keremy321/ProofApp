{
  "name": "proofapp-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "blockchain",
    "frontend"
  ],
  "scripts": {
    "compile": "npm --prefix blockchain run compile",
    "test": "npm --prefix blockchain run test",
    "deploy:local": "npm --prefix blockchain run deploy:local",
    "deploy:sepolia": "npm --prefix blockchain run deploy:sepolia",
    "abi:sync": "npm --prefix blockchain run abi:sync",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint"
  }
}
